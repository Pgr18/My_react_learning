import { FC } from 'react';
import { TextField } from '../../components/textFIeld';

export const LoginPage: FC = () => {
    return (
        <>
            <TextField labelText = "My label" value = "something"/>
        </>
    );
};